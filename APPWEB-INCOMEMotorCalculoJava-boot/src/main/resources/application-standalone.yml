## Define propiedades de la aplicación en local
server:
  port: 8080
amiga:
  service:
    aaa:
      local:
        # Deshabilita el modo de autenticación para test
        enable-local-test: true
        json-sso-file: /local-sso-ext.json
    rest:
      logging:
        # Limita la información devuelta en el caso de errores.
        is-production: false
  data:
    jdbc:
      datasource:        
        primary:
          driverClassName: com.ibm.db2.jcc.DB2Driver
          url: jdbc:db2://axdesdb22.central.inditex.grp:50000/INDITEX
          user: ugesbul
          password: bjtmqg
        secondary:
          driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
          url: jdbc:sqlserver://axinsqldes.central.inditex.grp:1433;databaseName=PeopleNet
          user: People7
          password: people7
    hibernate:
      primary:
        properties: '{''hibernate.dialect'':''com.inditex.rrhh.icmclcwb.model.dialect.DB2NativeDialect'',''hibernate.hbm2ddl.auto'':''validate'',''hibernate.show_sql'':''true'',''hibernate.format_sql'':''true''}'
      secondary:
        properties: '{''hibernate.dialect'':''com.inditex.rrhh.icmclcwb.model.dialect.SQLServerNativeDialect'',''hibernate.hbm2ddl.auto'':''validate'',''hibernate.show_sql'':''true'',''hibernate.format_sql'':''true''}'
app:
  # Variables de entorno propias de la aplicación
  envars:
    meta4:
      login:
        server: "https://axdeswas19.central.inditex.grp/RRHHAPPWSC-Income/services/Login"
      income:
        server: "https://axdeswas19.central.inditex.grp/RRHHAPPWSC-Income/services/ICM_WS_INCOME"