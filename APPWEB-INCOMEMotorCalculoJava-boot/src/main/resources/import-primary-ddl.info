/** Generar aumento de cache Identity
SELECT
        'ALTER TABLE ' CONCAT
        A.TABSCHEMA CONCAT
        '.' CONCAT
        A.TABNAME CONCAT
        ' ALTER COLUMN ' CONCAT
        A.COLNAME CONCAT
        ' SET CACHE 100;'
FROM SYSCAT.COLIDENTATTRIBUTES A
LEFT JOIN SYSCAT.SEQUENCES B
        ON B.SEQID = A.SEQID
WHERE A.TABSCHEMA = 'DESARROLLO_RRHH'
AND (A.TABNAME LIKE '%TRABAJO%' OR A.TABNAME LIKE '%PROGRAMACION%' OR A.TABNAME LIKE '%POC%');
*/