spring:
  profiles:
    active: test
jta:
  atomikos:
    properties:
      log-base-name: ${spring.application.name}-test1-log
amiga:
  common:
    cache:
      embedded:
        enabled: true
  service:
    aaa:
      security:
        headers:
          frame-options: DISABLE
      local:
        enable-local-test: true
    session:
      session-type: EMBEDDED