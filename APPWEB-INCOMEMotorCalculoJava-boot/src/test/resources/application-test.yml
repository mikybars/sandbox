spring:
  profiles:
    active: test
logging:
  level:
    root: WARN
    com.inditex.rrhh.icmclcwb: INFO
amiga:
  common:
    cache:
      embedded:
        enabled: true
  service:
    aaa:
      security:
        headers:
          frame-options: DISABLE
      local:
        enable-local-test: true
    session:
      session-type: EMBEDDED    