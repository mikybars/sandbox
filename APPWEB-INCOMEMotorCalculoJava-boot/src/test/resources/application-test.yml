spring:
  profiles:
    active: test
amiga:
  common:
    cache:
      embedded:
        enabled: true
  service:
    aaa:
      local:
        enable-local-test: true
    session:
      session-type: EMBEDDED