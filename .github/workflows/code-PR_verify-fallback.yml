---
name: code-PR-verify-fallback

on:
  pull_request:

env:
  WORKFLOW_VERSION: 2.57.0

jobs:
  unit-tests:
    if: 'false'
    name: Code / Verify
    runs-on: ${{ fromJSON(vars.RUNSON_CODE_GENERIC || '["citool-github__ubuntu24.04-medium"]') }}
    steps:
      - run: 'echo "No Code / Verify required"'
