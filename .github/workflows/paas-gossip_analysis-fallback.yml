---
name: paas-gossip-analysis-fallback

on:
  pull_request:

env:
  WORKFLOW_VERSION: 3.4.1

jobs:
  gossip-analysis:
    if: 'false'
    name: Gossip / Analysis
    runs-on: ${{ fromJSON(vars.RUNSON_GOSSIP_GENERIC || '["ubuntu-24.04"]') }}
    steps:
      - run: 'echo "No Gossip / Analysis required"'
