---
name: code-sonarqube-fallback

on:
  pull_request:

env:
  WORKFLOW_VERSION: 2.41.1

jobs:
  sonar-check:
    if: 'false'
    name: SonarQube Code Analysis
    runs-on: ${{ fromJSON( vars.RUNSON_SONARQUBE_GENERIC || '["citool-github__ubuntu24.04-medium"]' ) }}
    steps:
      - run: 'echo "No SonarScanner required"'
