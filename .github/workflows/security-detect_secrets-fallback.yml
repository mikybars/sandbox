---
name: security-detect_secrets-fallback

on:
  pull_request:

env:
  WORKFLOW_VERSION: 2.5.1

jobs:
  detect-secrets-pr:
    if: 'false'
    name: DSecrets / PR
    runs-on: ${{ fromJSON(vars.RUNSON_DSECRETS_ONPREM || '["citool-github__ubuntu24.04-medium"]') }}
    steps:
      - run: 'echo "DSecrets / PR required"'
