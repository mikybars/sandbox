app:
  context_path: /
  health_probe:
    initial_delay_seconds: 80
  has_spnego: true
  services:
    http:
      route:
        tls:
          enabled: true
          insecure_termination_policy: Redirect
        #timeout: 600
datagrid:
  application_cache:
    enabled: true
  # TODO (sesion) Eliminar para probar conexión Meta4
  session:
    enabled: true
amq:
  enabled: true