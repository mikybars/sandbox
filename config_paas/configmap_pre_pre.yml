amiga:
  common:
    rest:
      client:
        ptr-venta:
          base-url: https://pre-openshift.axpreocp1.central.inditex.grp/rhvtimpwsd
          auth-method: BASIC
          username: srvcicmclcwbpreAX
        ptr-presencia:
          base-url: http://pre-openshift.axpreocp1.central.inditex.grp/rhpresnwsd
          auth-method: BASIC
          username: srvcicmclcwbpreAX
  data:
    jms:
      connection-factory:
        broker-escritura:
          xaConnectionFactory:
            brokerURL: tcp://middleware-amq-tcp:61616
            userName: usr-write
        broker-lectura:
          xaConnectionFactory:
            brokerURL: tcp://middleware-amq-tcp:61616
            userName: usr-read
    jdbc:
      datasource:
        primary:
          xa-properties:
            serverName: axpredb22.central.inditex.grp
            portNumber: 50000
            databaseName: INDITEX
            currentSchema: INCOME
            user: uincome
app:
  envars:
    ptr:
      venta:
        service:
          version: 1.0.34-rc.2
      presencia:
        service:
          version: 1.0.16-rc.5
    meta4:
      config:
        credentials:
          user: INCOME_CALC
      login:
        server: https://pre-icmclcws.axpreocp1.central.inditex.grp/services/Login
      icmwscalcincome:
        server: https://pre-icmclcws.axpreocp1.central.inditex.grp/services/ICM_WS_CALC_INCOME