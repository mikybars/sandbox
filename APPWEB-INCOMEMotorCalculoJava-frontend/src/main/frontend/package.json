{
	"name": "webName",
	"version": "webVersion",
	"description": "Amiga Framework Web",
	"scripts": {
		"build": "amg build",
		"build:watch": "yarn run build -- --watch",
		"check-package": "yarn run test && yarn run lint && yarn run build",
		"docs": "amg docs",
		"form-builder": "amg form-builder",
		"generate": "amg generate",
		"lint": "amg lint ./src ./test",
		"server": "amg server",
		"start": "yarn run server",
		"test": "amg test --global-test-utils -r test/helpers.js",
		"test:watch": "yarn run test -- --watch"
	},
	"devDependencies": {
		"amiga": "latest"
	},
	"eslintConfig": {
		"extends": "amiga"
	},
	"main": "index.js",
	"author": "INDITEX",
	"license": "MIT"
}
