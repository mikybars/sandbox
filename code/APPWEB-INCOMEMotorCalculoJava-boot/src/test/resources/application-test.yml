spring:
  activemq:
    #broker-url: vm://embedded?broker.persistent=false&useShutdownHook=false&broker.useJmx=false
    broker-url: vm://embedded?broker.persistent=false&broker.useShutdownHook=false&broker.useJmx=false
    #broker-url: vm://localhost:61616?broker.persistent=false&broker.useShutdownHook=false&broker.useJmx=false
    #broker-url: tcp://localhost:61616//embedded?broker.persistent=false&useShutdownHook=false&broker.useJmx=false
    in-memory: true
amiga:
  common:
    rest:
      client:
        health:
          ping-enabled: false
    metrics:
      type: influx
  service:
    aaa:
      local:
        enable-local-test: true
    session:
      session-type: EMBEDDED
    web:
      api:
        problem-details:
          enabled: false
  data:
    jms:
      connection-factory:
        broker-escritura:
          #brokerURL: failover:(vm://embedded)
          brokerURL: vm://embedded
          #brokerURL: tcp://localhost:61616
        broker-lectura:
          #brokerURL: failover:(vm://embedded)
          brokerURL: vm://embedded
          #brokerURL: tcp://localhost:61616
app:
  envars:
    meta4:
      config:
        pool:
          size: 1
